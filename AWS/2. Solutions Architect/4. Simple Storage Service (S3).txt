
S3 OVERVIEW

- What is S3
- S3 Basics
- Working with S3 Buckets
- Key Value Store
- Availability and Durability
- Characteristics
- Securing your Data
- Consistency Model

- What is S3
Object Storage - S3 provides secure, durable, highly scalable object storage
Scalable - S3 allows you to store and retrieve any amount of data from anywhere on the web at a very low cost
Simple - Easy to use, with a simple web service interface 

S3 Manages data as objects rather than in file systems or data blocks 
We can upload any file type you can think of to S3
Examples include photos, videos, code, documents and text files
Cannot be used to run an OS or Database

- S3 Basics
Unlimited Storage
Objects up to 5TB in size
S3 Buckets - Stores file in buckets that is similar to folders

- Working with S3 Buckets 
Universal Namespace - Globally unique
Example S3 urls - https://bucket-name.s3.Region.amazonaws.com/key-name
Uploading Files - HTTP 200 code if the upload was successful

- Key Value store
Key - Name of the object
Value - Data itself, which is made up of sequence of bytes
Version ID - Important for storing multiple versions of the same object
Metadata - Data about the data that you are storing like content-type, Last modified, etc

- Availability and Durability
The data is spread across multiple devices and facilities to ensure availability and durability
Availability - 99.95%-99.99%
Durability - 99.99% till 9 decimal places

S3 Standard is the default storage class

- Characteristics
Tiered Storage - S3 offers a range of storage classes designed for different use cases
Lifecycle Management - Define rules to automatically transition objects to a cheaper storage tier or delete objects that are no longer required after a set period of time
Versioning - With versioning, all versions of an object are stored and can be retrieved, including deleted objects

- Securing your Data
Server side Encryption - You can set default encryption on a bucket to encrypt all new objects when they are stored in a bucket
Access Control Lists (ACLs) - Define which AWS accounts or groups are granted access and the type of access. You can attach S3 ACLs to individual objects within a bucket
Bucket Policies - S3 bucket policies specify what actions are allowed or denied(eg allow user Alice to PUT but not DELETE objects in a bucket)

- Consistency Model
Strong Read-After-Write Consistency
After a successful write of a new object (PUT) or an override of an existing object, any subsequent read request immediately receives the latest version of the object
Strong consistency for list operations, so after a write, you can immediately perform a listing of the objects in a bucket with the changes reflected
___________________________________________________________________________________________________________________

SECURING YOUR BUCKET WITH S3 BLOCK PUBLIC ACCESS 

- Object ACLs vs Bucket Policies
- Demo: Create an S3 Bucket
- Tips

- Object ACLs vs Bucket Policies
ACLs work on an individual object level
Bucket Policies work on an entire bucket level

- Tips
Buckets are private by default 
___________________________________________________________________________________________________________________

HOSTING A STATIC WEBSITE USING S3

- Static websites on S3
- Automatic Scaling
- Console Demo

- Static website on S3
Static website such as .html sites
Dynamic websites are those that require database connections, cannot be hosted on S3

- Automatic Scaling
S3 scales automatically to meet demand. 
Many enterprises will put static websiteson S3 when they think there is going to be a large number of requests(eg for a movie preview)
___________________________________________________________________________________________________________________

VERSIONING OBJECTS IN S3

- What is Versioning
- Advantages
- Console Demo

- What is Versioning
Multiple versions of an object within S3

- Advantages
All Versions - All versions of an object are stored in S3. This includes all writes and even if you delete an object
Backup - Can be a great backup tool
Cannot be Disabled - Once enabled, versioning cannot be disabled - only suspended
Lifecycle Rules - Can be integrated with lifecycle rules
Supports MFA - Can support MFA(IMP how can we secure deleting of files)

- Console Demo
Select Bucket > Properties > Versioning > Enable
IMP - Previous versions are not public hence we will get access denied while viewing old files
If we delete file with versioning on, it dont delete but add a new version called deleted marker
If we delete the deleted marker, we get our file back(Restore)
___________________________________________________________________________________________________________________

S3 STORAGE CLASSES

- S3 Standard
- S3 Standard - Infrequent Access
- S3 One Zone - Infrequent Access
- S3 Intelligent Tiering
- S3 Glacier Options
- Performance across S3
- Storage Costs


- S3 Standard 
High Availability and Durability - Data is stored redundantly across multiple devices in multiple facilities. >= 3 AZs. 99.99% availability. 99.99... eleven 9s Durability
Designed for frequent access - Perfect for frequently accessed data 
Suitable for most workloads - The default storage class. Use case include websites, content distribution, mobile and gaming applications and big data analytics


- S3 Standard Infrequent Access
Rapid access - Used for data that is accessed less frequently but requires rapid access when needed
You Pay to Access Data - There is a low per-GB storage price and a per-GB retrieval fee
Use Case - Great for long term storage, backups and as a data store for disaster recovery files

- S3 One Zone - Infrequent Access
Like S3 Standard Infrequent Access, but data is stored redundantly within a single AZ
Costs 20% less than regular S3 Standard Infrequent Access
Great for long lived, infrequently accessed, non critical data

- S3 Intelligent Tiering
Sometimes i need to access my data frequently, and other times i will access it infrequently
Frequent and Infrequent Access - Automatically moves your data to the most cost effective tier based on how frequently you access each object

- S3 Glacier Options(3 Options)
You pay each time you access your data 
Use only for archieving data
Glacier is cheap storage
Optimized for data that is very infrequently accessed
1 - Glacier Instant Retrieval - Provides long term data archieving with instant retrieval time for your data 
2 - Glacier Flexible Retrieval - Ideal storage class for archieve data that is not required immediate access but needs the flexifility to retrieve large sets of data at no cost, such as backup or disaster recovery use case. Can be minutes or up to 12 hrs
3 - Glacier Deep Archieve - Cheapest storage class and designed for customers that retain data sets for 7-10 years or longer to meet customer needs and regulatory compliance requirements. The standard retrieval time is 12 hrs, and the bulk retrieval time is 48 hrs

- Performance Across S3
Image attached

- Storage Costs
https://aws.amazon.com/s3/pricing/
___________________________________________________________________________________________________________________

LIFECYCLE MANAGEMENT WITH S3

- What is Lifecycle Management
- Lifecycle management and Versioning
- Console Demo

- What is Lifecycle Management
Automates moving your objects between different storage tiers, thereby maximizing cost effectiveness
S3 Standard(Keep for 30 days) -> S3-IA(30 days) -> Glacier

Lifecycle Management with Versioning - To move different versions of objects to different storage tiers

S3 Bucket > Management > Lifecycle rules > Create > Move versions > Create
___________________________________________________________________________________________________________________

S3 OBJECTS LOCK AND GLACIER VAULT LOCK

- S3 Object Lock
- Governance Mode
- Compliance Mode 
- Retention Periods
- Legal Holds
- Glacier Vault Lock

- S3 Object Lock
Write Once Read Many(WORM) model
It can help prevent objects from being deleted or modified for a fixed amount of time or indefinitely.
Used to meet regulatory requirements that require WORM storage, or add a. extra layer of protection against object changes and deletion

- Governance Mode
Users cant overwrite or delete an object version or alter its lock settings unless they have special permissions
We can still grant some users permissions to alter the retention settings or delete the objects of necessary

- Compliance Mode
A protected object cannot be overwritten or deleted by any user, including the root user
Retention mode cant be changed and its retention period cant be shortened
Ensures that an object bersion cant be deleted or overwritten for the duration of retention period

- Retention Periods
Protects an object version for a fixed amount of time
When you place a retention period on an object version, Amazon S3 stores a timestamp in the objects versions metadata to indicate when the retention period expires
After retention period expires, the object version can be overwritten or deleted unless you also placed a legal hold on the object version

- Legal Holds
S3 Object Locks also allows you to place a legal hold on the object version.
Legal hold prevents an object bersion from being deleted or overwritten.
Does not have a retention period and remains effective until removed
Can be freely placed and removed by any user having following permission
s3:PutObjectLegalHold

- Glacier Vault Lock
Allows to easily deploy and enforce compliance controls for individual S3 Glacier vaults with a vault lock policy. 
We can specify controls such as WORM in a vault lock policy and the policy from future edits.
Once locked, the policy can no longer be changed
___________________________________________________________________________________________________________________

ENCRYPTING S3 OBJECTS

- Types of Encryption
- Enforcing server-side encryption
- Console Demo

- Types of Encryption
Encryption at Transit - SSL/TLS, HTTPS
Encryption at Rest - Server-Side encryption -
SSE-S3: S3 managed keys, using AES 256-bit encryption
SSE-KMS: AWS Key management service-managed keys
SSE-C: Customer Provided Keys
Encryption at Rest - Client-Side Encryption - You encrypt the files yourself before you upload them to S3

- Enforcing Server-Side Encryption
Enabled by default - All objects encrypted Using SSE-S3
Applies to all objects in S3 bucket

How can we enforce it ?
x-amz-server-side-encryption - If the files is to be encrypted at upload time, this parameter will be included in the request header of PUT request
Two options -
x-amz-server-side-encryption: AES256 - SSE-S3
x-amz-server-side-encryption: aws:kms - SSE-KMS
We can provide bucket policy that denies any S3 PUT request that does not include this parameter in request header.

- Console Demo
Create Bucket > Encryption check
___________________________________________________________________________________________________________________

OPTIMIZING S3 PERFORMANCE

- S3 Prefixes
- S3 Performance 
- Limitations with KMS
- S3 Performance: Uploads
- S3 Performance: Downloads

- S3 Prefixes
bucketname/folder1/subfolder1/myfile.jpg > /folder1/subfolder1 is the prefix
bucketname/folder1/myFile.jpg > /folder1 is the prefix

- S3 Performance 
S3 has extremely low latency. First byte out of S3 in 100-200 ms
3500 PUT/COPY/DELETE/POST req/sec/prefix
5500 GET/HEAD req/sec/prefix
So, More prefix means more Performance 

- Limitations with KMS
When we upload, we call GenerateDataKey in the KMS API
When we download, we call Decrypt in the KMS API

Uploading/Downloading will count towards the KMS quota
Currently we cannot request a quota increase for KMS
Region-specific, its either 5500 or 10000 or 30000 req/second

- S3 Performance: Uploads
Multipart upload -
Recommended for files over 100 MB
Required for files over 5GB
Parallellize uploads(increases efficiency)

- S3 Performance: Downloads
S3 Byte-Range Fetchers -
Parallellize downloads by specific byte ranges
If there is a failure in download, its for only a specific byte range
___________________________________________________________________________________________________________________

BACKING UP DATA WITH S3 REPLICATION

- S3 Replication
- Console Demo 

- S3 Replication 
Replicate objects from one bucket to another - Versioning must be enabled on both the source and destination buckets
Objects in an existing bucket are not replicated automatically - Once replication is turned on,all subsequent updated objects will be replicated automatically
Delete markers are not replicated by default - Deleting individual versions or delete markers will not be replicated
___________________________________________________________________________________________________________________

LAB: SET UP CROSS REGION S3 BUCKET REPLICATION 
- Console Demo
Create Two Bucket(Source and Destination). Versioning is needed for Replication 
Go to Source bucket > Management > Create Replication rules > Select destination > Create new role > Save 
Try Uploading file to Source, it will get replicated to destination as well

LAB: CREATE A STATIC WEBSITE USING S3 
- Console Demo
Create a S3 Bucket > Uncheck block all public access
Select bucket > Properties > Static website hosting > Enable and provide index.html and error.html
Upload index.html, error.html > Make bucket public
Permissions > Bucket Policy > following permissions
{
 "Version": "2012-10-17",
 "Statement": [
   {
    "Sid": "PublicReadGetObject",
    "Effect": "Allow",
    "Principal": "*",
    "Action": [
       "s3:GetObject"
    ],
    "Resource": "arn:aws:s3:::BUCKET_NAME/*"
    ]
   }
  ]
}

LAB: CREATING S3 BUCKETS, MANAGING OBJECTS, AND ENABLING VERSIONING
- Demo: Create an S3 Bucket
Entire S3 console is on global namespace
Create S3 > Upload Files > Make object publically accessible
Permissions > Uncheck block public access
Permissions of Object > Ownership > Enable ACLs
Select Object > Actions > Make public using ACLs
Enable Versioning
___________________________________________________________________________________________________________________

QUIZ -
- Which of the following is not true                                                                                      - A
  S3 buckets are public by default 
  There is no max size for an S3 bucket 
  In S3, your objects are automatically stored across multiple devices spanning a minimum of 3 AZs 
  S3 serves as a key-based object store                     
- Your boss instructs you to make your company's s3 buckets public so employees can download content when                 - Its a terrible idea and opens up huge security risk for your company 
they are at home. What do you do ?
- Why is versioning not enabled by default on new S3 buckets ?                                                            - It costs money 
- How does availability and durability differ in S3 ?                                                                     - Durability is ability of AWS to ensure data is properly stored, Availability is the ability to access your data
- S3 allows to host ____ websites directly from bucket with no other tools needed ?                                       - Static 
